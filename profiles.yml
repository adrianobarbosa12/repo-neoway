resolucao_neoway:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: resolucao-neoway
      dataset: reso_neoway
      threads: 1
      timeout_seconds: 300
      location: US
      keyfile: credentials.json
